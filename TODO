- Home-Verzeichnisse durch Verwendung eines custom fact für Puppet zugänglich
  machen
- LAN-Antrag für pcfstest.nwz.wwu.de
- Firefox-Startseite mit nützlichen Links für Fachschaftsarbeit erstellen
- Firefox: „Welcome to Firefox“-Startseite deaktivieren
- Firefox: Standard-Suchmaschine einrichten
- Scanner einrichten → Gibt es einen Linux-Treiber?
- Test, ob Rechner bereits der Domäne beigetreten ist, schlägt in der neuesten
  Version des realmd-Moduls fehl. Siehe
  https://github.com/walkamongus/realmd/issues/36
- Probleme mit Firefox/Thunderbird auf dem Netzlaufwerk?
  (Firefox: Addons können nicht installiert werden;
  Thunderbird: Stürzt beim Beenden des Programms ab?)
- Nutzer werden nur ihrer Hauptgruppe zugeordnet – warum fehlen die Zusatzgruppen?
  → Mit enumerate=True und ldap_use_tokengroups=False in sssd.conf funktioniert
    es. Auf anderen Distributionen (z.B. CentOS) scheint es aber auch ohne zu
    funktionieren.
  https://bugs.launchpad.net/ubuntu/+source/sssd/+bug/1317949
- Alle Nutzer zu den Gruppen cdrom und plugdev hinzufügen?
  https://wiki.ubuntu.com/Security/Privileges
- Drucker werden mit jeder Puppet-Ausführung neu angelegt
  https://github.com/leoarnold/puppet-cups/issues/12
- CUPS-Berechtigungen konfigurieren (/etc/cups/cupsd.conf)
  → Brauchen Nutzer überhaupt die Berechtigung, Drucker hinzuzufügen?
- FS-Platte mit ownCloud synchronisieren
- Beim Neustart des PC erscheinen in den Foreman-Berichten Fehlermeldungen der
  Form „Failed to open TCP connection to nwzpuppet.nwz.wwu.de:8140
  (getaddrinfo: Name or service not known)“

- Symbol für kabelgebundenes Netzwerk in GNOME Shell zeigt an, dass keine
  Verbindung besteht, obwohl das Kabel verbunden ist und die Netzwerk-/
  Internet-Verbindung funktioniert
- Symbole auf dem Desktop (ja/nein? org.gnome.nautilus.desktop)
- Soll außer Firefox/Thunderbird noch etwas auf das Netzlaufwerk synchronisiert
  werden? Falls ja: was?
- pam_mount scheint beim Login zweimal ausgeführt zu werden?
  („(rdconf1.c:598): pcre_exec: no match“… erscheint vor und nach MOTD)
- Wie Nachrichten von pam_mount „(rdconf1.c:598): pcre_exec: no match“…
  deaktivieren?
- „user“-Ressourcen in Puppet für Nutzer aus dem Active Directory anlegen?
  (Eigenes Puppet-Modul?)
- Können Dateiberechtigungen (Windows-ACL) auf den Netzlaufwerken (CIFS)
  angezeigt/bearbeitet werden?
- Das Betriebssystem eines Rechners, der mit einer selbst angelegten Version
  installiert wurde (z.B. Ubuntu_p2fsphys 17.04) wird nach der Installation
  in Foreman (durch die automatische Weitergabe von Systeminformationen an
  Foreman) auf das Betriebssystem mit dem „echten“ Namen (z.B. Ubuntu 17.04)
  umgeschaltet, wenn ein solches existiert.

